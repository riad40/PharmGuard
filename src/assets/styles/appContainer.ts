import { StyleSheet } from "react-native"

const appContainer = StyleSheet.create({
    container: {
        width: "100%",
        height: "100%",
        backgroundColor: "#e5e5e5",
        padding: 20,
        paddingBottom: 0,
    },
})

export default appContainer
